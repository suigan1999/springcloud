server:
  port: 10086  #端口
spring:
  application:
    name: eureka-server #注册的服务名称，会在eureka中显示
eureka:
  client:
    service-url:  # EurekaServer的地址，现在是自己的地址，如果是集群，需要加上其它Server的地址。
      defaultZone: http://127.0.0.1:10086/eureka
